import "./Form.css"
import React, {Component} from "react";

export default class Form extends Component{
    render(){
        return({
            <div className={}>
                <form className={}>

                </form>
            </div>

        })
    }
}
